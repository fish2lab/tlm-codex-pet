# Put locally sourced TLM assets here

Required filenames:

- `model.json` — one normal Bedrock entity geometry
- `texture.png` — its matching texture

These files are ignored by Git. Obtain them from a source you are allowed to
use and redistribute. Do not assume every community model pack uses the same
license as Touhou Little Maid itself.
